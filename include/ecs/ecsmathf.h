#pragma once
#include "Components.h"
#include "mathf.h"

//inline bool CircleCircleOverlap(Transform& c1, Transform& c2) {
//	return pow((c2.position.x - c1.position.x), 2) + pow((c2.position.y - c1.position.y), 2) <= pow((c1.radius + c2.radius), 2);
//}
//
//inline bool CirclePointOverlap(Transform& circle, Vector2& point) {
//	return pow((point.x - circle.position.x), 2) + pow((point.y - circle.position.y), 2) <= circle.radius * circle.radius;
//}
